# Presentation results

Start with `summary/final_summary_page.png`, then use `common_errors/` and `expert_comparisons/`. All chart data are beside their PNGs as source CSV files. No training or inference was run.
