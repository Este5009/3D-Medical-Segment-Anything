{
  "encoder_config": "configs/rs2net_encoder.yaml",
  "initial_checkpoint": "outputs/higher_resolution_true_level0/checkpoints/best_higher_resolution_true_level0.pt",
  "verify_config": "configs/unetr_style_level0_full_width.yaml",
  "output_directory": "outputs/unetr_style_level0_full_width_augmented_decoder",
  "camri_split": "outputs/generalization_pilot/subject_split.json",
  "camri_metrics": "outputs/generalization_pilot/metrics.csv",
  "mouse_split": "outputs/mouse_boundary_adaptation/split.json",
  "mouse_metrics": "outputs/mouse_external_evaluation/subject_metrics.csv",
  "camri_cache": "/tmp/rs2_higher_resolution_true_level0_features",
  "mouse_cache": "/tmp/rs2_higher_resolution_true_level0_features",
  "corrected_cache": "/tmp/rs2_higher_resolution_true_level0_features",
  "raw_train_cache": "/tmp/rs2_full_width_augmented_raw_train_pairs",
  "seed": 20260902,
  "learning_rate": 0.00005,
  "weight_decay": 0.0001,
  "max_epochs": 40,
  "max_epochs_note": "Raised from 35 to 40: richer augmentation makes each training step see more varied/harder inputs, typically needing more epochs to fully converge (and each epoch also costs more now, since the frozen encoder must run fresh every step -- no more cached-feature shortcut for training).",
  "early_stop_patience": 5,
  "minimum_validation_improvement": 0.0003,
  "camri_validation_max_drop": 0.01,
  "loss": {"name": "dice_bce_boundary", "boundary_weight": 0.25, "boundary_width": 1},
  "paper_style_augmentation": {},
  "tile_size": [192, 128, 160],
  "model_spacing_mm": [0.16666666666666666, 0.20000000298023224, 0.1599999964237213],
  "architecture_change": "none. Sole variable relative to outputs/unetr_style_level0_full_width_decoder: training augmentation. That run's small, locally-confident false-positive 'bulbs' escaping the true contour on some subjects are consistent with a higher-capacity decoder (444,449 params) memorizing patterns from only 39 real training images. This run replaces the prior feature-space flip+noise trick with the original RS2-Net paper's own richer recipe (rotation, zoom, Gaussian blur/noise, brightness/contrast, gamma, simulated low-resolution), applied to raw images before the frozen encoder -- the standard remedy for this failure mode. Same initial_checkpoint, same architecture (level0_width=32), same resolution, same loss/optimizer/seed as the run being followed up on.",
  "label_resampling": {"is_seg": true, "order": 0, "order_z": 0},
  "inference_threshold": 0.5,
  "filter_connectivity": 26,
  "level0_width": 32,
  "batch_size": 1
}
